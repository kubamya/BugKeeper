<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.login.mapper.LoginMapper">
    <select id="login" parameterType="java.util.Map" resultType="java.util.Map">
		select c_bh as cBh, c_username as  cUsername, c_password as cPassword from t_user
        where c_username = #{username, jdbcType=VARCHAR} and c_password = #{password, jdbcType=VARCHAR}
	</select>

</mapper>
